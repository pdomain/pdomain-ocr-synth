# 10 — Publishing to Hugging Face

This spec describes how `pdomain-ocr-synth` publishes a rendered dataset to a
Hugging Face Datasets repo. It implements the producer half of the
workspace-wide contract in
[`/workspaces/ocr-container/DATASETS.md`](../../../DATASETS.md). Read that
first; this doc covers only the synth-specific details.

## When to publish

Publish is **explicit and separate from render**. Render writes the local
`pd-ocr-trainer/v1` layout for fast trainer iteration; publish takes that
output and ships it to HF for sharing or cloud training.

```bash
pdomain-ocr-synth render gaelic           # local; fast iteration
pdomain-ocr-synth publish gaelic          # one-shot upload to HF
```

Publishing requires that a render has already happened — it does not
re-render. Pass `--render-first` to chain them.

## What gets uploaded

`publish` reads `<destination>` from the recipe's `output:` block and
emits one HF dataset repo containing:

| Path | Source | Notes |
|------|--------|-------|
| `data/*.png` | `<destination>/images/` | Recognition mode |
| `data/*.parquet` | Built from local images + `labels.json` | Detection mode (parquet path; future) |
| `data/page_*.png` + `labels.json` | Copied verbatim from `<destination>/` | Detection mode (imagefolder path; current) |
| `metadata.jsonl` | Built from local `labels.json` + `manifest.jsonl` | Recognition only |
| `recipe.snapshot.yaml` | Copied from local output | Provenance |
| `README.md` | Generated; see "Dataset card" | Auto-generated |

Local-only files NOT uploaded: `manifest.jsonl` (rolled into
`metadata.jsonl`), `labels.json` (same), `stats.json` (rolled into the
README).

## Format conversion — recognition

Local layout:
```
images/0000000.png + labels.json entry {"0000000.png": "Séadna"} + manifest.jsonl record
```
becomes one row in HF `metadata.jsonl`:
```json
{"file_name": "data/0000000.png", "text": "Séadna",
 "font": "bungc/bungc.otf", "font_size_pt": 14.0,
 "degradations": ["skew", "paper_texture", "jpeg"],
 "corpus": "wikisource:mul:Séadna"}
```

The `file_name` column is the HF imagefolder convention. All other
columns are flat strings/numbers/lists; nothing nested.

## Format conversion — detection

Detection mode reads `labels.json` + the image files. Two upload paths
are supported:

- **Imagefolder (current).** `labels.json` is copied verbatim alongside
  the page images under `data/`. The trainer's `DetectionDataset`
  reader consumes the same `labels.json` schema documented in
  [08 — Output format](08-output-format.md), so the local pull path
  and the HF pull path are symmetrical. (An earlier draft of this
  section specified `pages.json`; the trainer's existing reader is
  the canonical contract — same precedent that landed `labels.json`
  over the original `pages.json`/`labels.csv` drafts in spec 08.)
- **Parquet (future).** `labels.json` projects into the schema below
  and ships as parquet shards via
  `datasets.Dataset.from_generator(...).push_to_hub(...)`. Not yet
  implemented (see spec 09 for sequencing); the imagefolder path is
  the contract until then.

Schema:
```python
{
  "image": Image(),
  "size": Sequence(Value("int32")),                # [width, height]
  "lines": Sequence({
    "bbox": Sequence(Value("int32"), length=4),
    "text": Value("string"),
    "words": Sequence({
      "bbox": Sequence(Value("int32"), length=4),
      "text": Value("string"),
    }),
  }),
  "font": Value("string"),
  "degradations": Sequence(Value("string")),
}
```

Shard size: 500 MB target per parquet file. The `datasets` library
handles splitting automatically.

## Dataset card

A `README.md` is generated with YAML front-matter:

```yaml
---
license: cc-by-4.0
task_categories: [text-recognition]
language: [ga]
tags: [ocr, gaelic, irish, pd-ocr, synthetic]
size_categories: [10K<n<100K]

# pd-ocr conventional keys (from DATASETS.md)
pd-ocr-shape: recognition/v1
pd-ocr-source: pdomain-ocr-synth
pd-ocr-recipe-sha: 2c4f...
pd-ocr-render-tool-version: 0.1.0
---

# pdomain-ocr-synth — gaelic

Synthetic Cló Gaelach + pre-reform Irish orthography OCR training data.

Generated by [pdomain-ocr-synth](https://github.com/...) from
`recipes/gaelic.yaml`. Reproduce with:

\`\`\`bash
pdomain-ocr-synth fetch gaelic
pdomain-ocr-synth render gaelic
pdomain-ocr-synth publish gaelic --repo <this-repo>
\`\`\`

## Stats
- Samples: 50000
- Fonts used: bungc, seangc, glangc, urgc, Gadelica, Cianchlo
- Tokens (unique): 8217
- Render time: 412s

## Provenance
- Recipe SHA: 2c4f...
- Tool version: pdomain-ocr-synth 0.1.0
- Corpus sources: CELT (G100001A, G100002, G102030), Wikisource
- Fonts: see recipe for licenses; not bundled
```

The synth tool fills in stats from `<destination>/stats.json` and
recipe summary from the resolved snapshot. Users can override the
generated README by placing `recipes/<recipe>/README.md.template`
next to the recipe.

## Recipe `publish:` block

Optional. Holds defaults so `publish` runs with no args.

```yaml
publish:
  hf_dataset:
    repo: ntw8532/pdomain-ocr-synth-gaelic
    private: false
    license: cc-by-4.0
    tags: [ocr, gaelic, irish, pd-ocr, synthetic]
    language: [ga]
    description_file: ./gaelic/README.md.template   # optional override
```

CLI flags (`--repo`, `--private`, `--license`, etc.) override recipe
values when both are present.

## Idempotency

Publish is **idempotent against repo state**:

1. Compute a content SHA over the local output (image bytes +
   metadata + recipe snapshot).
2. Read the latest commit's `card_data.pd-ocr-content-sha`.
3. If equal → exit 0 with "no changes" and do not commit.

This makes `pdomain-ocr-synth publish gaelic` safe to invoke from CI on
every run; only an actual recipe or output change triggers a new
commit.

## Versioning

Each publish creates one commit. Tag it explicitly when you want a
pin:

```bash
pdomain-ocr-synth publish gaelic --tag v2026.05.05
```

The trainer's profile config can reference the tag for a stable pin:

```yaml
sources:
  - hf://ntw8532/pdomain-ocr-synth-gaelic@v2026.05.05
```

`--message "..."` overrides the auto-generated commit message
(default: `pdomain-ocr-synth render @<recipe-sha>`).

## Authentication

Uses `huggingface_hub`'s default auth resolution:

1. `--token <token>` flag (least preferred; ends up in shell history)
2. `HF_TOKEN` env var (recommended)
3. `~/.cache/huggingface/token` (from `hf auth login`)

Public dataset writes need a token with `write` scope on the target
namespace. Read-only operations during render do not need a token.

## Tooling used

| Library / call | Purpose |
|---|---|
| `huggingface_hub.HfApi.create_repo` | First-time repo creation |
| `huggingface_hub.HfApi.upload_large_folder` | The actual upload (chunks into commits, resumes on failure) — used for recognition |
| `datasets.Dataset.from_generator(...).push_to_hub(...)` | Used for detection (parquet sharding) |
| `huggingface_hub.HfApi.list_repo_commits` | Idempotency check |
| `huggingface_hub.HfApi.create_tag` | `--tag` flag |

No custom upload code; all primitives come from the official libraries.

### Known limitation: `--message` with `upload_large_folder`

`HfApi.upload_large_folder` does **not accept** a `commit_message`
argument — its docstring (huggingface_hub ≥ 1.13) states "you cannot
set a custom `commit_message` and `commit_description` since multiple
commits are created." The chunked upload auto-generates per-shard
commit messages.

We accept `--message <MSG>` for forward-compatibility (a future
detection-mode parquet path may use `push_to_hub`, which honors it)
and so the orchestrator's commit-message logic lives at one site, but
the value **does not appear on the remote commit** for recognition
uploads. The CLI prints a single-line stderr warning when `--message`
is explicitly supplied so the gap is visible at run time. The
auto-generated default (`pdomain-ocr-synth render @<recipe-sha>`) is
likewise unstamped on the HF commit; we keep it because a future
switch to `upload_folder` (for small datasets) would honor it
verbatim.

## Dry run

`--dry-run` shows what would be uploaded without contacting HF:

```
$ pdomain-ocr-synth publish gaelic --dry-run
Would upload to: ntw8532/pdomain-ocr-synth-gaelic (public)
Files: 50003 (50000 .png + metadata.jsonl + README.md + recipe.snapshot.yaml)
Total size: 247.3 MB
Dataset card preview:
  ---
  license: cc-by-4.0
  pd-ocr-shape: recognition/v1
  ...
Content SHA: 2c4f8e... (no existing commit; first publish)
```

## Errors and recovery

| Failure | Behavior |
|---------|----------|
| Network error mid-upload | `upload_large_folder` resumes on retry; partial commit ok |
| Auth error | Exit code 7; print the auth resolution chain |
| Repo doesn't exist | Auto-create unless `--no-create` |
| Missing local render | Exit code 5 with "render first or use --render-first" |
| Local output corrupt (manifest mismatch) | Exit code 6; refuse to upload partial data |
| `--message` supplied | Stderr warning; flag accepted but `upload_large_folder` will not stamp it on the remote commit (see § Tooling used → Known limitation) |

## CLI summary

```bash
pdomain-ocr-synth publish <recipe>
  [--repo OWNER/NAME]      # default from recipe.publish.hf_dataset.repo
  [--private | --public]   # default from recipe; else public
  [--license LICENSE]
  [--tag VERSION]
  [--message MESSAGE]
  [--token TOKEN]
  [--render-first]         # render before publishing
  [--no-create]            # fail instead of auto-creating the repo
  [--dry-run]
```

See [01 — CLI](01-cli.md) for the full command catalog.
